# Validate an admove spatial reference

Internal helper used to ensure spatial reference objects are consistent.

## Usage

``` r
validate_sref(x)
```

## Arguments

- x:

  An `admove_sref` object.

## Value

The validated `admove_sref` (invisibly identical to input).
