% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tags.R
\name{check_tags}
\alias{check_tags}
\title{Check and standardise a tag object}
\usage{
check_tags(
  x,
  grid = NULL,
  dat = NULL,
  conf = NULL,
  remove_non_recovered_tags = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{x}{Tagging data to check. Can be an object of class \code{admove_tags}, a
data frame, a list of tag-specific data frames, or an object that contains
tags such as \code{admove_data}, \code{admove_sim}, or \code{admove}.}

\item{grid}{Optional spatial grid used to remove observations outside the
spatial domain and to assign grid cells.}

\item{dat}{Optional \code{admove_data} object used to check whether observations
fall within the time domain.}

\item{conf}{Optional configuration list. If provided, only tag types enabled
in \code{conf} are retained.}

\item{remove_non_recovered_tags}{Logical; if \code{TRUE}, remove tags with fewer
than two observations. Default: \code{TRUE}.}

\item{verbose}{Logical; if \code{TRUE}, print informative messages about removed
entries. Default: \code{TRUE}.}
}
\value{
A cleaned object of class \code{admove_tags}.
}
\description{
\code{check_tags()} validates tagging data, removes invalid entries, optionally
checks whether observations fall within the spatial and temporal domain of a
grid or data object, and returns a cleaned object of class \code{admove_tags}.

The function can be used on raw tagging data, on an \code{admove_tags} object,
or on higher-level \emph{admove} objects that contain tags.
}
\details{
The function performs several checks, including:
\itemize{
\item removal of rows with missing required values in \code{t}, \code{x}, or \code{y},
\item removal of rows with \code{use = FALSE},
\item removal of observations outside the spatial domain of \code{grid},
\item removal of observations outside the temporal domain of \code{dat}, and
\item optional removal of tags with fewer than two observations.
}

Spatial and temporal reference information are attached to the returned
object from \code{grid} and \code{dat} when available.
}
