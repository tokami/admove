% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates.R
\name{plot_cov}
\alias{plot_cov}
\alias{plot.admove_cov}
\alias{plot.admove_cov_list}
\title{Plot covariate fields}
\usage{
plot_cov(
  x,
  i = 1,
  select = NULL,
  main = "Covariate fields",
  labels = TRUE,
  plot_land = FALSE,
  auto_layout = TRUE,
  xlab = "x",
  ylab = "y",
  bg = NULL,
  plot_contour = TRUE,
  xlim = NULL,
  ylim = NULL,
  ...
)

\method{plot}{admove_cov}(x, ...)

\method{plot}{admove_cov_list}(x, ...)
}
\arguments{
\item{x}{An object of class \code{admove_cov}, or an object containing covariate
data such as \code{admove_data}, \code{admove_sim}, or \code{admove}.}

\item{i}{Index (scalar or vector) used when \code{x} is an \code{admove_cov_list}.
A scalar selects one covariate and plots all its time steps. A vector
selects multiple covariates and produces one panel per element (showing
the first time step, or the first element of \code{select}). Default: \code{1}.}

\item{select}{Optional vector of time-step indices to plot. Default:
\code{NULL}, in which case all time steps are plotted.}

\item{main}{Main title of the plot. Default: \code{"Covariate fields"}.}

\item{labels}{Logical; currently reserved for plotting cell labels. Default:
\code{TRUE}.}

\item{plot_land}{Logical; if \code{TRUE}, add land to the plot. Default:
\code{FALSE}.}

\item{auto_layout}{Logical; if \code{TRUE}, plotting parameters are set
automatically and restored afterwards. Default: \code{TRUE}.}

\item{xlab}{Label for the x-axis. Default: \code{"x"}.}

\item{ylab}{Label for the y-axis. Default: \code{"y"}.}

\item{bg}{Optional background colour for the plot. Default: \code{NULL}.}

\item{plot_contour}{Logical; if \code{TRUE}, add contour lines. Default:
\code{TRUE}.}

\item{xlim}{xlim}

\item{ylim}{ylim}

\item{...}{Additional graphical arguments passed to \code{\link[=plot]{plot()}}.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
\code{plot_cov()} plots one or more covariate fields stored in an \code{admove_cov}
object or in a higher-level \emph{admove} object that contains covariate
data.

Each time slice is plotted as a separate panel, optionally with contour
lines and land added to the plot.
}
\details{
The function uses the x and y dimension names, if present, as plotting
coordinates. Otherwise, row and column indices are used. Each selected time
slice is displayed with \code{\link[graphics:image]{graphics::image()}}, and optional contours are added
with \code{\link[graphics:contour]{graphics::contour()}}.
}
\examples{
plot_cov(skjepo$cov)

}
