% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_land}
\alias{plot_land}
\title{Plot land masses in the current plotting region}
\usage{
plot_land(
  sref = NULL,
  col = grDevices::adjustcolor(grey(0.7), 0.5),
  border = grey(0.5),
  download_map = FALSE,
  scale = 110,
  verbose = TRUE,
  warn_once = TRUE
)
}
\arguments{
\item{sref}{Optional spatial reference object, typically as returned by
\code{\link[=sref]{sref()}}. It should contain at least a valid CRS in \code{sref$crs}, and may
also contain plotting units in \code{sref$units} and a scaling factor in
\code{sref$crs_scale}.}

\item{col}{Fill colour for land polygons. Default is
\code{grDevices::adjustcolor(grey(0.7), 0.5)}.}

\item{border}{Border colour for land polygons. Default is \code{grey(0.5)}.}

\item{download_map}{Logical; if \code{TRUE}, the land map is downloaded if needed.
Otherwise, a locally available map is used when possible. Default is
\code{FALSE}.}

\item{scale}{Numeric map scale passed to the internal land-data loader.
Default is \code{110}.}

\item{verbose}{Logical; if \code{TRUE}, informative messages are printed.
Default is \code{TRUE}.}

\item{warn_once}{Logical; if \code{TRUE}, warnings about missing CRS information
are printed only once per session. Default is \code{TRUE}.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of adding land masses
to an existing plot.
}
\description{
Add land polygons to an existing plot, using the spatial reference stored in
\code{sref}. Land is transformed to the requested coordinate reference system,
optionally rescaled to match the plotting units, cropped to the current plot
extent, and then added to the active graphics device.
}
\details{
The function requires package \pkg{sf}. If \code{sref} does not contain a valid
CRS, no land is plotted. The land polygons are transformed to the requested
CRS, optionally multiplied by \code{sref$crs_scale}, and cropped to the current
plotting region defined by \code{par("usr")}.

For geographic coordinate systems, the plotting extent is truncated to valid
longitude and latitude ranges before cropping.
}
