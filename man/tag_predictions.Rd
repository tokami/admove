% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tag_pred.R
\name{tag_predictions}
\alias{tag_predictions}
\title{Predicted and observed tag positions}
\usage{
tag_predictions(fit, type = c("osa", "forecast"), i = NULL, verbose = TRUE)
}
\arguments{
\item{fit}{A fitted object of class \code{admove}, as returned by \code{\link[=admove]{admove()}}.}

\item{type}{Which prediction to return:
\describe{
\item{\code{"osa"}}{One step ahead (the default): each observation is
predicted from the one before it, as in the likelihood. For
mark-recapture tags, which are not updated, this is the prediction
from release to recapture.}
\item{\code{"forecast"}}{From release: the track is never updated on the
observations, so the prediction for an archival tag is a genuine
forecast over the whole time at liberty. Needs a second model build
and is therefore slower.}
}}

\item{i}{Optional tag indices (as in \code{\link[=add_tag_dist]{add_tag_dist()}}) or tag ids to restrict
the table to. For \code{type = "forecast"} a small subset is much faster,
because only those tags are built.}

\item{verbose}{Logical; if \code{TRUE} (default), messages about progress are
printed.}
}
\value{
A \code{data.frame} with one row per predicted observation and the columns
\describe{
\item{\code{id}, \code{tag_type}}{Tag id and type.}
\item{\code{obs}}{Index of the observation within the tag.}
\item{\code{t}, \code{date}}{Time of the observation, in model units and as a date.}
\item{\code{horizon}}{Time since the previous observation (\code{"osa"}) or since
release (\code{"forecast"}).}
\item{\code{x}, \code{y}}{Observed position.}
\item{\code{x_from}, \code{y_from}}{Position the prediction starts from: the previous
observation (\code{"osa"}) or the release (\code{"forecast"}).}
\item{\code{pred_x}, \code{pred_y}}{Predicted position.}
\item{\code{sd_x}, \code{sd_y}}{Prediction standard deviations.}
\item{\code{res_x}, \code{res_y}, \code{dist}}{Observed minus predicted, and the distance
between the two positions.}
\item{\code{z_x}, \code{z_y}, \code{d2}}{Standardised residuals and their squared sum.}
\item{\code{inside_50}, \code{inside_95}}{Whether the observation falls inside the
50\% / 95\% prediction ellipse.}
\item{\code{logdens}}{Log predictive density of the observation.}
\item{\code{type}}{The \code{type} argument, so tables can be combined.}
}
}
\description{
Returns, for every tag observation, the position the model predicts and the
position that was observed, together with the prediction uncertainty and
standardised residuals. This is the table behind \code{\link[=plot_tag_pred]{plot_tag_pred()}},
\code{\link[=plot_tag_resid]{plot_tag_resid()}} and \code{\link[=summarise_tag_pred]{summarise_tag_pred()}}.

The predictions are the ones the likelihood itself evaluates, reported by
\code{nll()} rather than recomputed, so they include the estimated observation
error, the per-tag-type update rules (\code{conf$do_update}) and the boundary
treatment (\code{conf$kf_boundary}).
}
\details{
Predicted positions are only available for the Kalman filter engine
(\code{conf$engine = 1}), which tracks a mean and a variance per tag. The CTMC
engine carries a distribution over grid cells instead; use \code{\link[=add_tag_dist]{add_tag_dist()}}
and \code{\link[=plot_tag_dist]{plot_tag_dist()}} for it.

The prediction is Gaussian with independent x and y, so \code{z_x} and \code{z_y}
should behave like standard normal draws and \code{d2 = z_x^2 + z_y^2} like a
chi-squared variable with 2 degrees of freedom. \code{inside_50} and \code{inside_95}
compare \code{d2} with its quantiles, i.e. they say whether the observation falls
inside the 50\% and 95\% prediction ellipse.

The first observation of each tag is its release: it is conditioned on, not
predicted, and therefore not in the table.
}
\examples{
\donttest{
fit <- admove(skjepo$sim)
pred <- tag_predictions(fit)
head(pred)
summarise_tag_pred(pred)
}

}
\seealso{
\code{\link[=plot_tag_pred]{plot_tag_pred()}}, \code{\link[=plot_tag_resid]{plot_tag_resid()}}, \code{\link[=summarise_tag_pred]{summarise_tag_pred()}}
}
