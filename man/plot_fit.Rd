% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{plot_fit}
\alias{plot_fit}
\alias{plot.admove}
\title{Summary plots for a fitted \code{admove} object}
\usage{
plot_fit(
  x,
  quantity = c("pref", "taxis", "dif", "par"),
  plot_land = FALSE,
  auto_layout = TRUE,
  col = .admove_cols(10),
  cor_dif = NULL,
  cor_tax = NULL,
  asp = 2,
  plot.legend = 1,
  bg = NULL,
  ...
)

\method{plot}{admove}(x, ...)
}
\arguments{
\item{x}{A fitted object of class \code{admove}, as returned by \code{\link[=admove]{admove()}}.}

\item{quantity}{Character vector specifying which quantities to plot.
Available options are:
\describe{
\item{\code{"pref"}}{Habitat preference functions.}
\item{\code{"taxis"}}{Taxis (movement direction and magnitude).}
\item{\code{"dif"}}{Diffusion.}
\item{\code{"par"}}{Estimated model parameters.}
}
Multiple quantities can be selected.}

\item{plot_land}{Logical; if \code{TRUE}, land masses are added to spatial plots
using \code{\link[=plot_land]{plot_land()}}. Default: \code{FALSE}.}

\item{auto_layout}{Logical; if \code{TRUE}, graphical parameters are set and
restored automatically, and plots are arranged in a multi-panel layout.
Default: \code{TRUE}.}

\item{col}{Colours used in the plots. Defaults to \code{.admove_cols(10)}.}

\item{cor_dif}{Optional scaling factor for diffusion symbols. If \code{NULL},
a default scaling is used internally.}

\item{cor_tax}{Optional scaling factor for taxis arrows. If \code{NULL},
a default scaling is used internally.}

\item{asp}{Positive numeric value specifying the target aspect ratio
(columns / rows) for the plot layout. Default: \code{2}.}

\item{plot.legend}{Integer controlling legend placement. If \code{1} (default),
a shared legend is drawn in a separate panel below the plots. If \code{2},
the legend is added to the final plot panel.}

\item{bg}{Optional background colour for the plotting device. If \code{NULL}
(default), the current background setting is used.}

\item{...}{Additional arguments}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of producing plots.
}
\description{
\code{plot_fit()} creates one or several diagnostic summary plots for a fitted
object of class \code{admove}. Depending on the selected \code{quantity}, the function
visualises habitat preference functions, taxis, diffusion, or parameter
estimates. Multiple quantities are arranged automatically in a multi-panel
layout.
}
\details{
The function is a wrapper around \code{\link[=plot_compare_one]{plot_compare_one()}} and is designed for
quick visual inspection of fitted models. If \code{auto_layout = TRUE}, the
plotting layout is determined automatically using \code{\link[=n2mfrow]{n2mfrow()}}, and graphical
parameters are reset after plotting.
}
\seealso{
\code{\link[=plot_compare_one]{plot_compare_one()}}
}
