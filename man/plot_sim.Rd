% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{plot_sim}
\alias{plot_sim}
\alias{plot.admove_sim}
\title{Plot a summary of simulated admove data}
\usage{
plot_sim(
  x,
  auto_layout = TRUE,
  plot_land = FALSE,
  cor_taxis = NULL,
  cor_diffusion = NULL,
  asp = 2,
  by_tag_type = TRUE,
  ...
)

\method{plot}{admove_sim}(x, ...)
}
\arguments{
\item{x}{An object of class \code{"admove_sim"}, as returned by
\code{\link[=sim_data]{sim_data()}}.}

\item{auto_layout}{Logical; if \code{TRUE}, the function automatically sets
and restores graphical parameters.}

\item{plot_land}{Logical; if \code{TRUE}, land masses are added to spatial
plots.}

\item{cor_taxis}{Optional scaling factor for taxis arrows.}

\item{cor_diffusion}{Optional scaling factor for diffusion arrows.}

\item{asp}{Positive numeric value giving the target aspect ratio
(columns / rows) of the plot arrangement.}

\item{by_tag_type}{Logical; if \code{TRUE}, simulated tracks are plotted
separately for each tag type. If \code{FALSE}, all tracks are shown in a
single panel.}

\item{...}{Additional arguments passed to lower-level plotting functions.}
}
\value{
Invisibly returns \code{NULL}. The function is called for its plotting side
effects.
}
\description{
Produces a set of summary plots for an object of class \code{"admove_sim"}.
Depending on the contents of the simulation object, the plots may include a
covariate field, habitat preference function, taxis, diffusion, and the
simulated tag tracks.
}
\details{
When \code{auto_layout = TRUE}, the function arranges multiple panels in a
suitable plotting layout. The exact set of panels depends on the simulation
object and whether simulated tag data are available.

If simulated tags are present and \code{by_tag_type = TRUE}, a separate plot
is produced for each tag type.
}
