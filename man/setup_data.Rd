% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{setup_data}
\alias{setup_data}
\title{Set up input data for an admove model}
\usage{
setup_data(
  grid = NULL,
  cov = NULL,
  tags = NULL,
  trange = NULL,
  knots_tax = NULL,
  knots_dif = NULL,
  n_knots_tax = 3,
  n_knots_dif = 1,
  sref = NULL,
  tref = NULL,
  transform_sref = FALSE,
  shift_tref = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{grid}{Optional grid object, typically of class \code{admove_grid}, as
returned by \code{\link[=create_grid]{create_grid()}}. A grid is required for some model engines and
for spatial prediction and plotting.}

\item{cov}{Optional covariate object or list of covariates. Covariates are
typically prepared with \code{\link[=prep_cov]{prep_cov()}}. If a single covariate is supplied,
it is coerced internally to a list.}

\item{tags}{Optional tag data, typically as returned by one or more of
\code{\link[=prep_ctags]{prep_ctags()}}, \code{\link[=prep_dtags]{prep_dtags()}}, or \code{\link[=prep_stags]{prep_stags()}}. Several tag objects can
be supplied combined with \code{c(dtags, ctags)} or as a list,
\code{list(dtags, ctags)}; both are merged with \code{\link[=combine_tags]{combine_tags()}}.}

\item{trange}{Optional numeric vector of length two giving the model time
range. If \code{NULL}, the time range is inferred from available tags and
covariates.}

\item{knots_tax}{Optional matrix of spline knots for the taxis preference
functions, with knots in rows and one column per covariate. If \code{NULL},
\code{n_knots_tax} knots are placed at covariate quantiles.}

\item{knots_dif}{Optional matrix of spline knots for the diffusion
preference functions, with knots in rows and one column per covariate. If
\code{NULL}, \code{n_knots_dif} knots are placed at covariate quantiles.}

\item{n_knots_tax}{Number of default knots per covariate for the taxis
preference functions. Default is \code{3}. Ignored if \code{knots_tax} is supplied.}

\item{n_knots_dif}{Number of default knots per covariate for the diffusion
preference functions. Default is \code{1}, i.e. constant diffusion. Ignored if
\code{knots_dif} is supplied.}

\item{sref}{Optional spatial reference to use as the target spatial
reference for all inputs. If supplied, it should be coercible to an
\code{admove_sref} object.}

\item{tref}{Optional time reference to use as the target time reference for
all inputs. If supplied, it should be coercible to an \code{admove_tref}
object.}

\item{transform_sref}{Logical; if \code{TRUE}, spatial components are
transformed or rescaled to a common spatial reference where possible. If
\code{FALSE} (default), all spatial references must already match.}

\item{shift_tref}{Logical; if \code{TRUE}, temporal components are shifted or
harmonised to a common time reference where possible. If \code{FALSE}
(default), all time references must already match.}

\item{verbose}{Logical; if \code{TRUE}, informative messages are printed during
processing. Default is \code{TRUE}.}
}
\value{
An object of class \code{admove_data}, containing the processed grid, covariates,
tags, spline knots, prediction settings, and associated spatial and temporal
reference information.
}
\description{
Combine the main input components for an \code{admove} analysis into a single
object of class \code{admove_data}. The function checks and harmonises spatial and
temporal references, prepares covariates and tags, defines spline knots,
and constructs default prediction grids and time points used during fitting
and plotting.
}
\details{
The function first determines a common spatial reference (\code{sref}) and time
reference (\code{tref}) either from the user-supplied targets or from the input
objects. If \code{transform_sref = FALSE} or \code{shift_tref = FALSE}, all inputs
must already be compatible. Otherwise, inputs are harmonised to the chosen
target references.

If \code{trange} is not supplied, it is inferred from the union of tag and
covariate time ranges. If no valid time range can be determined, a default
range of \code{c(0, 1)} is used.

If spline knots are not supplied, default knots are placed at quantiles of
the marginal covariate distributions: \code{n_knots_tax} knots per covariate for
taxis (default three: the 5\%, 50\% and 95\% quantiles) and \code{n_knots_dif} knots
for diffusion (default one: the median, i.e. constant diffusion). Two knots
are placed at the 25\% and 75\% quantiles, four at the 5\%, 30\%, 70\% and 95\%
quantiles, and five or more evenly between the 5\% and 95\% quantiles. One
knot gives a constant function and two knots a linear one (a natural cubic
spline through two points is a straight line). All covariates share the
same number of knots. The spline coefficients created by \code{\link[=default_par]{default_par()}}
are sized from these knots, so set the number of knots here rather than
editing \code{dat$knots_tax} or \code{dat$knots_dif} afterwards.

The returned object also contains default prediction components in
\code{dat$pred}, including:
\describe{
\item{\code{pred$time}}{A sequence of 10 prediction time points over \code{trange}.}
\item{\code{pred$cov}}{Prediction ranges for each covariate.}
\item{\code{pred$grid}}{The prediction grid, if a grid was supplied.}
}

Additional defaults used later during fitting are also stored in the output,
including \code{eps}, \code{var_init_kf}, \code{log2steps}, \code{min_dt}, \code{dt}, and \code{p_init}.
}
\examples{
ctags <- prep_tags(
  skjepo$ctags,
  tag_type = "c",
  names = c(
    t0 = "date_time", t1 = "date_caught",
    x0 = "rel_lon",   x1 = "recap_lon",
    y0 = "rel_lat",   y1 = "recap_lat"
  ),
  date_origin = "1899-12-30")

## prepare data-storage tags
dtags <- prep_tags(
  skjepo$dtags,
  tag_type = "d",
  names = c(t = "time", x = "mptlon", y = "mptlat"),
  date_origin = "1899-12-30")

grid <- create_grid(x = skjepo$grid, cellsize = c(10, 10))

cov <- prep_cov(skjepo$cov)

dat <- setup_data(
  grid = grid,
  cov = cov,
  tags = c(dtags, ctags),
  transform_sref = TRUE,
  shift_tref = TRUE
)

## five taxis knots and a linear diffusion function
dat5 <- setup_data(
  grid = grid,
  cov = cov,
  tags = c(dtags, ctags),
  n_knots_tax = 5,
  n_knots_dif = 2,
  transform_sref = TRUE,
  shift_tref = TRUE
)

}
