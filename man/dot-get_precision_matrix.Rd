% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{.get_precision_matrix}
\alias{.get_precision_matrix}
\title{Construct a precision matrix for a grid}
\usage{
.get_precision_matrix(grid, h, nu, rho, delta, matern = TRUE)
}
\arguments{
\item{grid}{A grid object of class \code{"admove_grid"}, as returned by
\code{\link[=create_grid]{create_grid()}}.}

\item{h}{Distance argument used internally in the Matérn covariance
structure.}

\item{nu}{Smoothness parameter of the Matérn covariance structure.}

\item{rho}{Spatial range parameter of the Matérn covariance structure.}

\item{delta}{Small positive value added to the diagonal to ensure numerical
stability and positive definiteness.}

\item{matern}{Logical; if \code{TRUE}, a Matérn-based covariance structure is
used. Otherwise, a neighbour-based graph Laplacian is constructed.}
}
\value{
A precision matrix for the active cells of the grid.
}
\description{
Constructs a spatial precision matrix for an \code{admove_grid}, either from
a Matérn covariance structure or from a simple neighbour-based structure.
}
\details{
When \code{matern = TRUE}, pairwise distances between grid-cell centres are
used to build a Matérn covariance matrix, which is then inverted to obtain a
precision matrix. When \code{matern = FALSE}, a sparse neighbour-based
precision matrix is constructed from the grid adjacency structure.

In both cases, \code{delta * I} is added to the diagonal.
}
\keyword{internal}
