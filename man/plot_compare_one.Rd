% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_compare_one}
\alias{plot_compare_one}
\title{Compare a single quantity across fitted or simulated admove objects}
\usage{
plot_compare_one(
  fit,
  ...,
  quantity = c("pref", "taxis", "dif", "par"),
  plot_land = FALSE,
  auto_layout = TRUE,
  asp = 2,
  col = .admove_cols(10),
  lty = 1:10,
  cor_tax = NULL,
  cor_dif = NULL,
  plot.legend = 1,
  panel_lab = NULL,
  bg = NULL
)
}
\arguments{
\item{fit}{An object of class \code{admove} or \code{admove_sim}, or a list of such
objects.}

\item{...}{Additional \code{admove} or \code{admove_sim} objects to compare.}

\item{quantity}{Character string specifying which quantity to compare.
Currently implemented options are:
\itemize{
\item \code{"pref"} for habitat preference functions,
\item \code{"taxis"} for taxis,
\item \code{"dif"} for diffusion, and
\item \code{"par"} for parameter estimates.
}}

\item{plot_land}{Logical; if \code{TRUE}, add land masses to spatial plots using
\code{\link[=plot_land]{plot_land()}}. Default: \code{FALSE}.}

\item{auto_layout}{Logical; if \code{TRUE}, restore graphical parameters on exit.
Default: \code{TRUE}.}

\item{asp}{Positive numeric value giving the target aspect ratio
(columns / rows) of the plot arrangement. Default: \code{2}.}

\item{col}{Vector of colours used for the different objects being compared.
By default, colours are taken from \code{.admove_cols(10)}.}

\item{lty}{Vector of line types used for the different objects being
compared. Default: \code{1:10}.}

\item{cor_tax}{Optional scaling factor for taxis arrows. If \code{NULL}
(default), the longest arrow is automatically scaled to one grid cell
width.}

\item{cor_dif}{Optional scaling factor for diffusion symbols. If \code{NULL}
(default), the largest circle is automatically scaled to one grid cell
width.}

\item{plot.legend}{Currently not used inside the function body.}

\item{bg}{Optional background colour for the plotting device. Default:
\code{NULL}.}
}
\value{
No return value. Called for its side effect of producing a comparison plot.
}
\description{
\code{plot_compare_one()} provides a compact plotting interface for comparing a
single quantity across multiple fitted or simulated \emph{admove} objects.
Depending on the selected \code{quantity}, the function compares habitat
preference functions, taxis, diffusion, or parameter estimates.

The input can be supplied either as individual \code{admove} / \code{admove_sim}
objects or as a list of such objects.
}
\details{
For \code{quantity = "pref"}, habitat preference curves are overlaid in a single
panel. For \code{quantity = "taxis"} and \code{quantity = "dif"}, spatial movement
components are compared on the prediction grid. For \code{quantity = "par"},
fitted parameter estimates are shown, with confidence intervals for fitted
\code{admove} objects where available.

Simulated objects of class \code{admove_sim} can be included in the comparison,
allowing direct visual comparison between simulated and fitted quantities.
}
