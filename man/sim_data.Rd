% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_data}
\alias{sim_data}
\title{Simulate a complete admove data set}
\usage{
sim_data(
  grid = NULL,
  cov = NULL,
  par = NULL,
  dat = NULL,
  conf = NULL,
  fit = NULL,
  trange = NULL,
  dt = NULL,
  simulate_cov = FALSE,
  simple = FALSE,
  nt = NULL,
  rho_t = 0.85,
  sd = 2,
  h = 0.2,
  nu = 2,
  rho_s = 0.8,
  delta = 0.1,
  zrange = c(20, 28),
  matern = TRUE,
  sim_buffer = TRUE,
  knots_tax = NULL,
  knots_dif = NULL,
  release_events = NULL,
  n_release_events = 10,
  trange_rel = NULL,
  xrange_rel = NULL,
  yrange_rel = NULL,
  trange_rec = NULL,
  use_dtags = TRUE,
  use_ctags = TRUE,
  use_stags = FALSE,
  n_dtags = 10,
  n_stags = 0,
  n_ctags = 100,
  n_resightings = c(1, 5),
  sim_engine = 1,
  use_reject = FALSE,
  n_reject = 100,
  target_dif_frac = 1/500,
  target_tax_frac = 1/10,
  target_sdO_frac = 1/30,
  plot = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{grid}{Optional spatial grid used for simulation. If not already an
\code{admove_grid}, it is converted using \code{\link[=create_grid]{create_grid()}}.}

\item{cov}{Optional covariate fields. If \code{NULL}, or if
\code{simulate_cov = TRUE}, covariates are simulated internally using
\code{\link[=sim_cov]{sim_cov()}}.}

\item{par}{Optional named list of simulation parameters. Missing parameters
are filled using \code{\link[=default_sim_par]{default_sim_par()}}.}

\item{dat}{Optional \code{admove_data} object used as a template for the
simulation. If supplied, missing inputs such as grid, covariates, time
range, and spline knots are extracted from it.}

\item{conf}{Optional configuration list for the movement model.}

\item{fit}{Optional fitted \code{admove} model. If supplied, the simulation
can reuse information from the fitted model, such as the original data
object.}

\item{trange}{Numeric vector of length 2 giving the simulation time range.
If \code{NULL}, a default range is used or extracted from \code{dat}.}

\item{dt}{Optional model time-step size.}

\item{simulate_cov}{Logical; if \code{TRUE}, covariate fields are simulated
even when covariates are already available from \code{dat} or \code{fit}.}

\item{simple}{Logical; if \code{TRUE}, simple deterministic covariate fields
are simulated instead of random fields.}

\item{nt}{Number of covariate fields (time steps) to simulate.}

\item{rho_t}{Temporal autocorrelation coefficient for simulated covariate
fields.}

\item{sd}{Standard deviation used in the simulation of covariate fields.}

\item{h}{Parameter controlling the covariate precision or covariance
structure.}

\item{nu}{Smoothness parameter of the Matérn covariance structure.}

\item{rho_s}{Spatial range parameter of the Matérn covariance structure.}

\item{delta}{Small positive value added for numerical stability in the
precision matrix.}

\item{zrange}{Numeric vector of length 2 giving the target range of the
simulated covariate values.}

\item{matern}{Logical; if \code{TRUE}, a Matérn-based covariance structure is
used for covariate simulation.}

\item{sim_buffer}{Logical; if \code{TRUE}, the simulation grid is extended by
a one-cell buffer before covariates are simulated.}

\item{knots_tax}{Optional knot locations for the taxis component.}

\item{knots_dif}{Optional knot locations for the diffusion component.}

\item{release_events}{Optional data frame or matrix of release events. If
\code{NULL}, release events are simulated internally using
\code{\link[=sim_release_events]{sim_release_events()}}.}

\item{n_release_events}{Number of release events to simulate if
\code{release_events} is not supplied.}

\item{trange_rel}{Optional time range within which releases occur.}

\item{xrange_rel}{Optional x-range within which release locations are drawn.}

\item{yrange_rel}{Optional y-range within which release locations are drawn.}

\item{trange_rec}{Optional time range within which recapture or final
observation times are drawn.}

\item{use_dtags}{Logical; if \code{TRUE}, data-storage tags are simulated.}

\item{use_ctags}{Logical; if \code{TRUE}, conventional mark-recapture tags
are simulated.}

\item{use_stags}{Logical; if \code{TRUE}, mark-resight tags are simulated.}

\item{n_dtags}{Number of data-storage tags to simulate.}

\item{n_stags}{Number of mark-resight tags to simulate.}

\item{n_ctags}{Number of conventional mark-recapture tags to simulate.}

\item{n_resightings}{Integer vector giving the minimum and maximum number of
resightings for mark-resight tags.}

\item{sim_engine}{Integer specifying the simulation engine: \code{1} for
continuous-space simulation and \code{2} for CTMC-based grid simulation.}

\item{use_reject}{Logical; if \code{TRUE}, invalid simulated locations are
rejected and redrawn where relevant.}

\item{n_reject}{Maximum number of rejection attempts used in rejection-based
simulation steps.}

\item{target_dif_frac}{Target diffusion strength as a fraction of the
characteristic spatial scale squared per unit time, used by
\code{\link[=default_sim_par]{default_sim_par()}}.}

\item{target_tax_frac}{Target taxis strength as a fraction of the
characteristic spatial scale per unit time, used by \code{\link[=default_sim_par]{default_sim_par()}}.}

\item{target_sdO_frac}{Target observation error as a fraction of the
characteristic spatial scale, used by \code{\link[=default_sim_par]{default_sim_par()}}.}

\item{plot}{Logical; if \code{TRUE}, a summary plot of the simulated data is
produced.}

\item{verbose}{Logical; if \code{TRUE}, informative messages are printed.}
}
\value{
An object of class \code{"admove_sim"} containing the simulated grid,
covariates, simulated tags, simulation parameters, \code{admove_data}
object, and default fitting components.
}
\description{
Simulates a complete data set for \code{admove}, including a spatial grid,
optional covariate fields, simulated tags, and the corresponding
\code{admove_data} object required for model fitting.
}
\details{
The function provides a convenient way to generate a complete simulation
setup for \code{admove}. Depending on the supplied inputs, it can reuse an
existing grid, covariates, or fitted model, or simulate these components
from scratch.

Covariates are simulated with \code{\link[=sim_cov]{sim_cov()}} if needed. Tag release events are
either provided directly or generated with \code{\link[=sim_release_events]{sim_release_events()}}. Tag data
for the requested tag types are then simulated using \code{\link[=sim_tags]{sim_tags()}}. Finally,
the simulated tags and covariates are combined into an \code{admove_data}
object suitable for model fitting.

The returned object also includes default configuration, parameter, and map
objects for downstream fitting with \code{\link[=admove]{admove()}}.
}
\examples{
sim <- sim_data()

}
