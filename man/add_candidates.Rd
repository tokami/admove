% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tags.R
\name{add_candidates}
\alias{add_candidates}
\title{Attach candidate recapture locations to tags}
\usage{
add_candidates(x, candidates, by = c(id = "id"), names = NULL, verbose = TRUE)
}
\arguments{
\item{x}{An object of class \code{admove_tags} (or one containing tags, such as
\code{admove_data}).}

\item{candidates}{A data frame of candidate positions, one row per
candidate, with a column identifying the tag.}

\item{by}{Named character scalar linking tags to candidates, given as
\verb{c(<tags column> = "<candidates column>")}, e.g. \code{c(id = "REC_ID")}. A
single unnamed string is used for both sides.}

\item{names}{Named character vector giving the candidate columns:
\code{c(t = "...", x = "...", y = "...", prob = "...")}. \code{prob} is optional; if
omitted the candidates of a tag are treated as equally likely.}

\item{verbose}{Logical; if \code{TRUE}, report how many tags were matched.
Default: \code{TRUE}.}
}
\value{
An object of class \code{admove_tags} carrying \code{event} and \code{prob} columns. Tags
with no entry in \code{candidates} are returned unchanged, with their single
recapture at probability 1.
}
\description{
\code{add_candidates()} replaces the final observation of each tag with a set of
candidate positions, turning a single assumed recapture into an ambiguous
one. This is the situation where the recapturing vessel is known but the
individual set is not: each of the vessel's fishing sets is a possible
recapture location, with a probability derived from its effort.

Use this when the candidates arrive as their own long table, one row per
candidate, keyed by tag. When they arrive as repeated columns of a wide
table instead (\code{date1}, \code{lat1}, \code{lon1}, \code{per1}, \code{date2}, ...), map them
directly in \code{\link[=prep_tags]{prep_tags()}} via \code{candidates} and skip this function.
}
\details{
Probabilities must already sum to 1 within each tag; convert an effort
measure beforehand, for example \code{prob = hours / sum(hours)} per tag. The
check itself happens in \code{\link[=check_tags]{check_tags()}}, together with the rule that only the
final observation of a tag may be ambiguous.
}
\examples{
## tags <- add_candidates(ctags, sets,
##                        by = c(id = "REC_ID"),
##                        names = c(t = "set_date", x = "set_lon",
##                                  y = "set_lat", prob = "set_prob"))

}
\seealso{
\code{\link[=prep_tags]{prep_tags()}}, \code{\link[=check_tags]{check_tags()}}
}
