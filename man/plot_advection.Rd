% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_advection}
\alias{plot_advection}
\title{Plot advection on a spatial grid}
\usage{
plot_advection(
  x,
  select = NULL,
  select_sea = NULL,
  average = TRUE,
  cor = NULL,
  col = "black",
  alpha = 0.5,
  lwd = 1,
  main = "Advection",
  plot_land = FALSE,
  image_bg = TRUE,
  auto_layout = TRUE,
  add = FALSE,
  xlab = "x",
  ylab = "y",
  xaxt = "s",
  yaxt = "s",
  bg = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class \code{admove} or \code{admove_sim}.}

\item{select}{Optional index vector specifying which prediction time steps to
plot. If \code{NULL}, all available prediction time steps are used.}

\item{select_sea}{Optional index vector selecting which seasonal components to
plot when the advection coefficients are seasonal. If \code{NULL}, all seasons
are shown.}

\item{average}{Logical; if \code{TRUE} (default), the advection vectors are
averaged over the selected time steps. If \code{FALSE}, advection is plotted
separately for each selected time step.}

\item{cor}{Optional scaling factor for arrow lengths. If \code{NULL}, the longest
arrow is automatically scaled to one grid cell width.}

\item{col}{Colour of the arrows. Default is \code{"black"}.}

\item{alpha}{Transparency value. Currently not used directly in the plotting
call. Default is \code{0.5}.}

\item{lwd}{Line width of the arrows. Default is \code{1}.}

\item{main}{Main title of the plot. Default is \code{"Advection"}.}

\item{plot_land}{Logical; if \code{TRUE}, land masses are added using
\code{\link[=plot_land]{plot_land()}}. Default is \code{FALSE}.}

\item{image_bg}{Logical; if \code{TRUE} (default), a colour image of advection
magnitude is drawn underneath the arrows.}

\item{auto_layout}{Logical; if \code{TRUE}, the plotting layout is set
automatically. If multiple time steps are plotted and \code{average = FALSE},
panels are arranged using \code{\link[=n2mfrow]{n2mfrow()}}. Default is \code{TRUE}.}

\item{add}{Logical; if \code{TRUE}, advection arrows are added to an existing plot.
If \code{FALSE} (default), a new plot is created.}

\item{xlab}{Label for the x-axis. Default is \code{"x"}.}

\item{ylab}{Label for the y-axis. Default is \code{"y"}.}

\item{xaxt}{A character specifying the x-axis type, passed to \code{\link[=plot]{plot()}}.
Default is \code{"s"}.}

\item{yaxt}{A character specifying the y-axis type, passed to \code{\link[=plot]{plot()}}.
Default is \code{"s"}.}

\item{bg}{Optional background colour for the plotting device. If \code{NULL}
(default), the current background setting is used.}

\item{...}{Additional arguments passed to \code{\link[=plot]{plot()}} when a new plot is
created.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of producing a plot.
}
\description{
Plot the advection component as arrows over the spatial prediction grid for a
fitted or simulated \code{admove} object. Advection is the directed transport of
the animal by the environmental flow (e.g. ocean currents), scaled by the
estimated entrainment coefficients \code{gamma}; the arrows therefore show
\code{gamma * current} in coordinate units per time step. The function can display
advection at selected time steps or the average across multiple time steps.
}
\details{
For objects of class \code{admove}, the function plots predicted advection from
\code{x$pred$hAx} and \code{x$pred$hAy} (which already incorporate the estimated
\code{gamma}). For objects of class \code{admove_sim}, advection is recomputed from the
simulated covariates and parameter values.

If \code{average = TRUE}, the mean advection over the selected time steps is
plotted. Otherwise, one panel per selected time step is produced unless
\code{add = TRUE}.
}
\seealso{
\code{\link[=plot_taxis]{plot_taxis()}}, \code{\link[=plot_diffusion]{plot_diffusion()}}
}
