% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tags.R
\name{plot_tags}
\alias{plot_tags}
\alias{plot.admove_tags}
\title{Plot tagging data}
\usage{
plot_tags(
  x,
  main = "Tags",
  plot_land = FALSE,
  auto_layout = TRUE,
  xlim = NULL,
  ylim = NULL,
  add = FALSE,
  xlab = "x",
  ylab = "y",
  leg_pos = "topright",
  labels = FALSE,
  bg = NULL,
  by_tag_type = TRUE,
  by_tag = FALSE,
  col = c(adjustcolor("grey60", 0.3), .admove_cols(2)),
  pch = c(1, 0, 16),
  cex = 0.8,
  ...
)

\method{plot}{admove_tags}(x, ...)
}
\arguments{
\item{x}{An object of class \code{admove_tags}, or an object containing tagging
data such as \code{admove_data}, \code{admove_sim}, or \code{admove}.}

\item{main}{Main title of the plot. Default: \code{"Tags"}.}

\item{plot_land}{Logical; if \code{TRUE}, add land to the plot. Default: \code{FALSE}.}

\item{auto_layout}{Logical; if \code{TRUE}, plotting parameters are set
automatically and restored afterwards. Default: \code{TRUE}.}

\item{xlim}{Optional x-axis limits.}

\item{ylim}{Optional y-axis limits.}

\item{add}{Logical; if \code{TRUE}, add to an existing plot. Default: \code{FALSE}.}

\item{xlab}{Label for the x-axis. Default: \code{"x"}.}

\item{ylab}{Label for the y-axis. Default: \code{"y"}.}

\item{leg_pos}{Position of the legend. Default: \code{"topright"}.}

\item{labels}{Logical; if \code{TRUE}, label observations by time instead of
plotting intermediate points. Default: \code{FALSE}.}

\item{bg}{Optional background colour for the plot. Default: \code{NULL}.}

\item{by_tag_type}{Logical; if \code{TRUE}, create separate panels by tag type.
Default: \code{TRUE}.}

\item{by_tag}{Logical; if \code{TRUE}, create separate panels for individual tags.
Default: \code{FALSE}.}

\item{col}{Character vector of length 1 to 3 giving colours for tag paths,
release positions, and recovery or final observation positions.}

\item{...}{Additional graphical arguments passed to \code{\link[=plot]{plot()}}.}
}
\value{
No return value. Called for its side effect of producing a plot.
}
\description{
\code{plot_tags()} plots tagging data in space, showing release locations,
intermediate observations where available, and final recovery or resight
locations.

Depending on the tag type, tags are drawn either as trajectories connecting
successive observations or as straight lines between release and recovery.
}
\details{
Data-storage and mark-resight tags with intermediate observations are plotted
as trajectories through space. Conventional mark-recapture tags are plotted
as straight-line segments between release and recovery positions.

When \code{by_tag_type = TRUE}, separate panels are created for each tag type
present in the data. When \code{by_tag = TRUE}, each tag is shown in a separate
panel.
}
\examples{
plot_tags(skjepo$sim$tags)

}
