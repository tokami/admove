% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_tag_pred}
\alias{plot_tag_pred}
\title{Plot predicted against observed tag positions}
\usage{
plot_tag_pred(
  x,
  i = NULL,
  type = NULL,
  pred = NULL,
  level = 0.95,
  n_ellipse = 6L,
  link = TRUE,
  plot_land = TRUE,
  plot_grid = TRUE,
  col_obs = "grey20",
  col_pred = "dodgerblue3",
  xlab = NULL,
  ylab = NULL,
  ...
)
}
\arguments{
\item{x}{A fitted object of class \code{admove}, as returned by \code{\link[=admove]{admove()}}.}

\item{i}{Tag indices or tag ids to show. \code{NULL} (default) uses every tag in
\code{pred}, or all archival tags when \code{type = "forecast"} and \code{pred} is not
supplied.}

\item{type}{Prediction to show, passed to \code{\link[=tag_predictions]{tag_predictions()}}: \code{"forecast"}
(from release, the default for a single tag) or \code{"osa"} (one step ahead,
the default for several tags).}

\item{pred}{Optional table from \code{\link[=tag_predictions]{tag_predictions()}}, to avoid recomputing
it. Must contain the requested tags.}

\item{level}{Confidence level of the prediction ellipses and intervals.
Default \code{0.95}.}

\item{n_ellipse}{Maximum number of prediction ellipses drawn along a single
tag's track. The observations they belong to are marked and dated. Default
\code{6}.}

\item{link}{How to join an observation to its own prediction: \code{TRUE}
(default) joins the dated pairs, \code{"all"} joins every observation to its
prediction (a dense fan for a long track), \code{FALSE} draws no lines.}

\item{plot_land}{Logical; if \code{TRUE} (default), land masses are added.}

\item{plot_grid}{Logical; if \code{TRUE} (default), the model grid is outlined.}

\item{col_obs, col_pred}{Colours of the observed and predicted positions.}

\item{xlab, ylab}{Axis labels of the map panel. Default to the spatial units.}

\item{...}{Additional arguments passed to \code{\link[=plot]{plot()}} for the map panel.}
}
\value{
Invisibly returns the prediction table that was plotted.
}
\description{
Compares the positions the model predicts with the observed ones, from the
table returned by \code{\link[=tag_predictions]{tag_predictions()}}.

With a single tag the plot has three panels: a map with the observed and the
predicted track and prediction ellipses, and the x and y coordinate against
time with the prediction interval. With several tags it is a single map with,
per tag, a line from the starting position to the observed position and one
to the predicted position, which is the natural view for mark-recapture tags.

Every prediction belongs to one observation, at the same time: the predicted
track therefore ends at the time of the last observation. A few pairs along
the track are marked, dated and joined by a line, and their times are marked
in the coordinate panels too, so that points can be matched between panels.
Only the release (conditioned on, not predicted) and observations excluded
from the fit have no prediction.
}
\seealso{
\code{\link[=tag_predictions]{tag_predictions()}}, \code{\link[=plot_tag_resid]{plot_tag_resid()}}, \code{\link[=plot_tag_dist]{plot_tag_dist()}}
}
