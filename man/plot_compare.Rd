% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_compare}
\alias{plot_compare}
\title{Compare fitted and simulated admove objects}
\usage{
plot_compare(
  fit,
  ...,
  quantity = c("pref", "taxis", "dif", "par"),
  plot_land = FALSE,
  auto_layout = TRUE,
  col = .admove_cols(10),
  cor_dif = NULL,
  cor_tax = NULL,
  asp = 2,
  plot.legend = 1,
  bg = NULL
)
}
\arguments{
\item{fit}{Either a single object of class \code{admove} or \code{admove_sim}, or a
list of such objects. If a named list is supplied, names are used in the
legend.}

\item{...}{Additional \code{admove} or \code{admove_sim} objects to compare.}

\item{quantity}{Character vector specifying which quantities to compare.
Implemented options are:
\describe{
\item{\code{"pref"}}{Habitat preference.}
\item{\code{"taxis"}}{Taxis.}
\item{\code{"dif"}}{Diffusion.}
\item{\code{"par"}}{Parameter estimates.}
}
Multiple quantities can be selected.}

\item{plot_land}{Logical; if \code{TRUE}, land masses are added to spatial plots
using \code{\link[=plot_land]{plot_land()}}. Default is \code{FALSE}.}

\item{auto_layout}{Logical; if \code{TRUE}, the plot layout and graphical
parameters are set automatically. Default is \code{TRUE}.}

\item{col}{Colours used for the different objects being compared. Defaults to
\code{admove:::.admove_cols(10)}.}

\item{cor_dif}{Optional scaling factor for diffusion symbols. If \code{NULL},
the default internal scaling is used.}

\item{cor_tax}{Optional scaling factor for taxis arrows. If \code{NULL},
the default internal scaling is used.}

\item{asp}{Positive numeric value giving the target aspect ratio
(columns / rows) for the plot arrangement. Default is \code{2}.}

\item{plot.legend}{Logical or integer controlling legend placement. If set
to \code{1} (default), a shared legend is drawn in a separate layout panel. If
set to \code{2}, the legend is added within the final plot panel.}

\item{bg}{Optional background colour for the plotting device. If \code{NULL}
(default), the current background setting is used.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of producing plots.
}
\description{
Create comparison plots for one or more fitted or simulated \code{admove}
objects. Depending on \code{quantity}, the function can compare habitat
preference, taxis, diffusion, or parameter estimates across objects.
Multiple requested quantities are arranged automatically in a multi-panel
layout.
}
\details{
If \code{auto_layout = TRUE}, the function arranges the requested comparison plots
automatically. For spatial quantities, land can optionally be added via
\code{\link[=plot_land]{plot_land()}}. Simulated objects of class \code{admove_sim} can be compared
directly with fitted objects of class \code{admove}.

When \code{plot.legend = 1}, a shared legend is drawn below the plots. If the
input objects are unnamed, fitted objects are labelled sequentially and
simulated objects are labelled \code{"Sim"}.
}
