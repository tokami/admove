% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{.admove_slot}
\alias{.admove_slot}
\title{Identify the argument an admove object belongs to}
\usage{
.admove_slot(x)
}
\arguments{
\item{x}{Any object.}
}
\value{
One of \code{"fit"} (a fitted model or a simulation), \code{"dat"} (a data
object), \code{"cov"} (covariate fields), or \code{"grid"} for the objects
\code{\link[=create_grid]{create_grid()}} can build a grid from. Anything else is an error, because
\code{\link[=create_grid]{create_grid()}} would silently fall back to its default grid.
}
\description{
Maps an object to the \code{sim_data()} argument it describes, so that any
admove object can be passed as the first argument \code{x}.
}
\keyword{internal}
