% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{time_2_date}
\alias{time_2_date}
\alias{time_2_date.numeric}
\alias{time_2_date.default}
\alias{time_2_date.admove_tags}
\alias{time_2_date.admove_data}
\alias{time_2_date.admove_cov}
\title{Convert numeric model time back to dates}
\usage{
time_2_date(x, tref = NULL, ...)

\method{time_2_date}{numeric}(x, tref = NULL, ...)

\method{time_2_date}{default}(x, tref = NULL, ...)

\method{time_2_date}{admove_tags}(x, tref = NULL, ...)

\method{time_2_date}{admove_data}(x, tref = NULL, ...)

\method{time_2_date}{admove_cov}(x, tref = NULL, ...)
}
\arguments{
\item{x}{A numeric vector of times since the origin, or an \code{admove} object
carrying a time vector: \code{admove_tags} (its \code{t}), \code{admove_data} (its
\code{tags$t}), or \code{admove_cov} (its layer times).}

\item{tref}{A time reference supplying \code{origin} and \code{units}. One of an
\code{admove_tref}, an \code{admove} object (its \code{\link[=tref]{tref()}} is used), a list with
elements \code{origin} and \code{units}, or a bare \code{Date}/\code{POSIXt} origin (units then
default to \code{"day"}). When \code{x} is an \code{admove} object and \code{tref} is \code{NULL},
\code{tref(x)} is used.}

\item{...}{Further arguments passed to methods.}
}
\value{
A \code{POSIXct} vector (or \code{Date} when \code{origin} is a \code{Date}) the same
length as the input time vector, with \code{NA} preserved.
}
\description{
Inverse of \code{\link[=date_2_time]{date_2_time()}}: given a numeric time scale measured since a
reference origin (for example the \code{t} column of an \code{admove_tags} object),
return the corresponding dates / date-times.
}
\details{
The conversion mirrors \code{\link[=date_2_time]{date_2_time()}} exactly. Fixed-length units
(\code{second}, \code{minute}, \code{hour}, \code{day}, \code{week}) use a constant number of seconds
per unit. Units \code{month} and \code{year} are calendar-aware (their length varies),
so the inverse is obtained by numerically inverting \code{\link[=date_2_time]{date_2_time()}} itself;
the round trip \code{date_2_time(time_2_date(t, tref), tref)} recovers \code{t} to
machine precision, and original dates are recovered exactly wherever
\code{\link[=date_2_time]{date_2_time()}} is injective (e.g. day-resolution data).
}
\examples{
tr <- create_tref("2003-01-01 UTC", units = "months")
time_2_date(c(9.33, 138.16), tref = tr)

## round-trips date_2_time()
d <- as.Date(c("2003-10-11", "2014-07-07"))
time_2_date(date_2_time(d, tref = tr), tref = tr)

}
\seealso{
\code{\link[=date_2_time]{date_2_time()}}
}
