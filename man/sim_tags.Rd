% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_tags}
\alias{sim_tags}
\title{Simulate tagging data}
\usage{
sim_tags(
  tag_type,
  grid = NULL,
  cov = NULL,
  par = NULL,
  dat = NULL,
  conf = NULL,
  n_tags = 1,
  n_resightings = c(1, 5),
  trange = NULL,
  dt_tags = NULL,
  trange_rel = NULL,
  xrange_rel = NULL,
  yrange_rel = NULL,
  n_release_events = 1,
  release_events = NULL,
  trange_rec = NULL,
  knots_tax = NULL,
  knots_dif = NULL,
  funcs = NULL,
  use_reject = FALSE,
  n_reject = 20,
  sim_engine = 1,
  ctmc_method = 2,
  sref = NULL,
  tref = NULL,
  target_dif_frac = 1/300,
  target_tax_frac = 1/10,
  target_sdO_frac = 1/30,
  add_obs_unc = NULL,
  plot = FALSE,
  plot_land = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{tag_type}{Character string specifying the tag type to simulate.
Supported values are \code{"d"} or \code{"dtags"} for data-storage tags,
\code{"s"} or \code{"stags"} for mark-resight tags, and \code{"c"} or
\code{"ctags"} for conventional mark-recapture tags.}

\item{grid}{Optional spatial grid used for simulation. If not already of
class \code{"admove_grid"}, it is converted using \code{\link[=create_grid]{create_grid()}}.}

\item{cov}{Optional covariate fields used to define spatially and temporally
varying movement rates.}

\item{par}{Optional named list of simulation parameters. Missing parameters
are filled using \code{\link[=default_sim_par]{default_sim_par()}}.}

\item{dat}{Optional \code{admove_data} object. If \code{NULL}, a default data
object is constructed internally from the supplied inputs.}

\item{conf}{Optional configuration list controlling the movement model. If
\code{NULL}, \code{\link[=default_conf]{default_conf()}} is used.}

\item{n_tags}{Number of tags to simulate.}

\item{n_resightings}{Integer vector giving the minimum and maximum number of
resightings for mark-resight tags.}

\item{trange}{Numeric vector of length 2 giving the simulation time range.
If \code{NULL}, it is derived from the covariate time dimension when
possible, or defaults to \code{c(0, 1)}.}

\item{dt_tags}{Optional simulation time step for tag trajectories. If
\code{NULL}, a default value of \code{0.1} is used.}

\item{trange_rel}{Optional time range within which release times are
generated.}

\item{xrange_rel}{Optional x-range within which release positions are
generated.}

\item{yrange_rel}{Optional y-range within which release positions are
generated.}

\item{n_release_events}{Number of release events to simulate if
\code{release_events} is not supplied.}

\item{release_events}{Optional data frame specifying release events. If
\code{NULL}, release events are generated internally.}

\item{trange_rec}{Optional time range within which recapture or final
observation times are generated.}

\item{knots_tax}{Optional knot locations for the taxis component.}

\item{knots_dif}{Optional knot locations for the diffusion component.}

\item{funcs}{Optional named list of simulation functions. If \code{NULL},
defaults are created with \code{\link[=default_sim_funcs]{default_sim_funcs()}}.}

\item{use_reject}{Logical; if \code{TRUE}, invalid movement proposals are
rejected and resampled.}

\item{n_reject}{Maximum number of rejection attempts per step if
\code{use_reject = TRUE}.}

\item{sim_engine}{Integer specifying the simulation engine: \code{1} for
continuous-space simulation and \code{2} for CTMC-based grid simulation.}

\item{ctmc_method}{Integer controlling the matrix-exponential method used for
CTMC simulation.}

\item{sref}{Optional spatial reference to attach to the simulated data.}

\item{tref}{Optional temporal reference to attach to the simulated data.}

\item{target_dif_frac}{Target diffusion strength as a fraction of the
characteristic spatial scale squared per unit time, used by
\code{\link[=default_sim_par]{default_sim_par()}}.}

\item{target_tax_frac}{Target taxis strength as a fraction of the
characteristic spatial scale per unit time, used by \code{\link[=default_sim_par]{default_sim_par()}}.}

\item{target_sdO_frac}{Target observation error as a fraction of the
characteristic spatial scale, used by \code{\link[=default_sim_par]{default_sim_par()}}.}

\item{add_obs_unc}{Add observation uncertainty to tag locations? By default
(NULL), observation uncertainty is added to archival tags (tag_type =
"d"), but not to other tags.}

\item{plot}{Logical; if \code{TRUE}, the simulated tags are plotted.}

\item{plot_land}{Logical; if \code{TRUE}, land masses are added to the plot.}

\item{verbose}{Logical; if \code{TRUE}, informative messages are printed.}
}
\value{
An object of class \code{"admove_sim"} containing the simulated tags, grid,
covariates, simulation parameters, data object, and default fitting
components.
}
\description{
Simulates tagging data under the \code{admove} movement model for one of the
supported tag types: data-storage tags, mark-resight tags, or conventional
mark-recapture tags.
}
\details{
The function first sets up the spatial grid, time range, covariates, and
model configuration required for simulation. If no data object is supplied,
one is created internally from the provided inputs.

Tag trajectories are then simulated from generated or user-supplied release
events. The full simulated trajectories are retained for data-storage tags.
For mark-resight tags, only the release and a subset of subsequent
observations are kept. For conventional mark-recapture tags, only the
release and final observation are retained.

The returned object contains both the simulated tags and the associated
simulation setup, including the grid, covariates, simulation parameters, and
internally constructed \code{admove_data} object.
}
\examples{
data(skjepo)
sim <- sim_tags("ctags", skjepo$grid)

}
