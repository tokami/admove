% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{.resolve_sim_inputs}
\alias{.resolve_sim_inputs}
\title{Resolve the objects a simulation is built from}
\usage{
.resolve_sim_inputs(
  x = NULL,
  grid = NULL,
  cov = NULL,
  par = NULL,
  dat = NULL,
  conf = NULL,
  fit = NULL,
  trange = NULL,
  knots_tax = NULL,
  knots_dif = NULL,
  n_knots_tax = NULL,
  n_knots_dif = NULL
)
}
\arguments{
\item{x}{Any admove object, or \code{NULL}. See \code{\link[=.admove_slot]{.admove_slot()}}.}

\item{grid, cov, par, dat, conf, fit, trange, knots_tax, knots_dif, n_knots_tax, n_knots_dif}{The arguments of the calling function, as supplied by the user.}
}
\value{
A named list with the resolved \code{grid}, \code{cov}, \code{par},
\code{dat}, \code{conf}, \code{fit}, \code{trange}, \code{knots_tax} and
\code{knots_dif}, plus the values the user supplied explicitly
(\code{par_in}, \code{conf_in}, \code{knots_tax_in},
\code{knots_dif_in}), which the caller needs when it re-simulates
covariate fields.
}
\description{
Routes the object given as \code{x} to the argument it describes, and fills
in whatever is still missing from a fitted model and its data object. This
is shared by \code{\link[=sim_data]{sim_data()}} and \code{\link[=sim_tags]{sim_tags()}} so that both merge their inputs
by the same rules.
}
\details{
Arguments given explicitly always win: values are taken from \code{fit}
and \code{dat} only where the caller left them empty. A data object
describes its own grid, covariates, knots and time range, so it is dropped
(and rebuilt by the caller) as soon as one of those is replaced. The knots
hold one column of covariate values per covariate and therefore only
survive a change of the covariate fields if the user supplied them. A
number of knots (\code{n_knots_tax}, \code{n_knots_dif}) that differs from
the inherited knots replaces them in the same way, unless a knot matrix was
supplied, which always wins.
}
\keyword{internal}
