% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates.R
\name{prep_cov}
\alias{prep_cov}
\title{Prepare covariate fields for admove}
\usage{
prep_cov(
  x,
  x_centers = NULL,
  y_centers = NULL,
  times = NULL,
  date_decimal = FALSE,
  date_format = NULL,
  date_origin = NULL,
  tz = "UTC",
  sref = NULL,
  tref = NULL,
  layers = NULL,
  plot = FALSE,
  plot_land = FALSE,
  strict = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{x}{Covariate data. Supported inputs include:
\itemize{
\item a \code{data.frame} with columns \code{x} and \code{y} (coordinates) and one
additional column per covariate; each row is one grid cell. The
covariate column names become the names of the returned
\code{admove_cov_list}. This is the natural output of GIS exports and
spatial model pipelines.
\item a list of matrices, typically one matrix per time step,
\item a 2D array or matrix, interpreted as a single time slice,
\item a 3D array with dimensions x, y, and time,
\item \code{RasterLayer}, \code{RasterBrick}, or \code{RasterStack} objects, and
\item \code{SpatRaster} objects.
}}

\item{x_centers}{Optional numeric vector giving x coordinates of cell
centres.}

\item{y_centers}{Optional numeric vector giving y coordinates of cell
centres.}

\item{times}{Optional vector giving the time values associated with the third
dimension.}

\item{date_decimal}{Logical; if \code{TRUE}, interpret time labels as decimal
years and convert them to model time. Default: \code{FALSE}.}

\item{date_format}{Optional character string passed to \code{\link[=as.Date]{as.Date()}} to parse
character time labels. Default: \code{NULL}.}

\item{date_origin}{Optional origin passed to \code{\link[=as.Date]{as.Date()}} when time labels are
stored numerically. Default: \code{NULL}.}

\item{tz}{Time zone used when converting dates. Default: \code{"UTC"}.}

\item{sref}{Optional spatial reference information to attach to the returned
object.}

\item{tref}{Optional time reference information to attach to the returned
object.}

\item{layers}{Character string controlling how layers in multi-layer
inputs are interpreted. Applies to \code{SpatRaster}, \code{RasterBrick},
\code{RasterStack}, lists of matrices, and 3-D arrays. Use \code{"covariates"} to
treat each layer as a separate covariate field; the function then returns
a named list of class \code{admove_cov_list}. Use \code{"time"} to treat layers as
consecutive time steps of a single covariate; the function then returns a
single \code{admove_cov}. Default (\code{NULL}) is \code{"covariates"} for
\code{SpatRaster}/\verb{Raster*} objects and \code{"time"} for lists and arrays, which
preserves backward-compatible behaviour for those types. Ignored for
single-layer inputs.}

\item{plot}{Logical; if \code{TRUE}, plot the prepared covariate field. Default:
\code{FALSE}.}

\item{plot_land}{Logical; passed to the plotting method when \code{plot = TRUE}.
Default: \code{FALSE}.}

\item{strict}{Logical; if \code{TRUE}, require stricter matching and validation of
dimension names. Default: \code{FALSE}.}

\item{verbose}{Logical; if \code{TRUE}, print informative messages. Default:
\code{TRUE}.}
}
\value{
When \code{layers = "time"} or when the input has only one layer, an
object of class \code{admove_cov}, stored as a 3D array with dimensions
corresponding to x, y, and time. When \code{layers = "covariates"} and
the input has multiple layers, a named list of class \code{admove_cov_list} with
one \code{admove_cov} element per layer, ready to pass directly to \code{\link[=setup_data]{setup_data()}}.
}
\description{
\code{prep_cov()} converts covariate data into the standard array-based format
used by \emph{admove}. Covariates can be supplied as a list of matrices, as a
2D or 3D array, or as raster-like objects. When the input has multiple
layers, the \code{layers} argument controls whether they are treated as
separate covariate fields (default, returns a named \code{admove_cov_list}) or
as consecutive time steps of a single covariate (returns a single
\code{admove_cov}).

The function can also attach spatial and temporal reference information,
convert date-like time labels to numeric model time, and optionally plot the
prepared covariate field.
}
\details{
If \code{x} is two-dimensional, it is converted to a 3D array with a single time
slice. Dimension names are validated and, where possible, inferred from the
input or from the optional \code{x_centers}, \code{y_centers}, and \code{times} arguments.

If date-like time labels are supplied, they can be converted to numeric model
time using \code{date_format}, \code{date_origin}, or \code{date_decimal}. Spatial and
temporal reference information are preserved from the input where available
and supplemented by \code{sref} and \code{tref} if provided. A user-supplied \code{sref}
takes precedence over any CRS derived from a Raster or SpatRaster object.
}
\examples{
cov <- prep_cov(skjepo$cov)

}
