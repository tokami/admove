% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sref.R
\name{scale_sref}
\alias{scale_sref}
\title{Change coordinate units by scaling an admove object}
\usage{
scale_sref(x, scale = 1, units = NULL, verbose = TRUE)
}
\arguments{
\item{x}{An \code{admove_grid}, \code{admove_*tags}, \code{admove_cov}, or
\code{admove_data} object.}

\item{scale}{Numeric scalar. Coordinates are multiplied by this factor.
For example, to convert meters to kilometers (numerically), use
\code{scale = 0.001}.}

\item{units}{units}

\item{verbose}{Logical; if \code{TRUE}, prints informational messages.}
}
\value{
\code{x} with rescaled coordinates and updated \code{sref(x)}.
}
\description{
Multiply the stored coordinates of an \code{admove_*} object by \code{scale}
and update its spatial reference accordingly. The spatial reference is
updated by:
\itemize{
\item multiplying \code{sref(x)$crs_scale} by \code{scale}
\item updating \code{sref(x)$units} where possible (e.g. m <-> km)
}
}
\examples{

grid <- create_grid(cellsize = 5e3, xrange = c(0, 5e4), yrange = c(0, 5e4))

grid <- add_sref(grid, list(crs = 32631, units = "m", crs_scale = 1))

grid <- scale_sref(grid, scale = 0.001)

units_space(grid)

crs_scale(grid)


}
