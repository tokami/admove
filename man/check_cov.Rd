% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates.R
\name{check_cov}
\alias{check_cov}
\title{Check and standardise covariate data}
\usage{
check_cov(x, verbose = TRUE)
}
\arguments{
\item{x}{Covariate data to check. Can be \code{NULL}, an object of class
\code{admove_cov} or \code{admove_cov_list}, or another object that can be converted
with \code{\link[=prep_cov]{prep_cov()}}.}

\item{verbose}{Logical; if \code{TRUE}, print informative messages about removed
covariate fields. Default: \code{TRUE}.}
}
\value{
\code{NULL} if no valid covariate field remains; otherwise an object of class
\code{admove_cov} or \code{admove_cov_list}.
}
\description{
\code{check_cov()} validates covariate data and ensures that it can be used by
\emph{admove}. If the input is not already of class \code{admove_cov} or
\code{admove_cov_list}, the function first tries to convert it using
\code{\link[=prep_cov]{prep_cov()}}.

Existing covariate objects are checked for missing values, missing dimension
names, and incompatible dimension-name lengths. Invalid covariate fields are
removed.
}
\details{
For existing covariate objects, the function removes covariate fields that:
\itemize{
\item contain only missing values,
\item have missing or incomplete dimension names, or
\item have dimension names whose lengths do not match the array dimensions.
}
}
