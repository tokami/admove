% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_pref_grid}
\alias{plot_pref_grid}
\title{Plot spatial habitat preference surfaces}
\usage{
plot_pref_grid(
  x,
  type = "taxis",
  select_cov = NULL,
  select.y = NULL,
  select.sea = NULL,
  combine_cov = FALSE,
  combine.sea = FALSE,
  main = NULL,
  col = hcl.colors(14, "YlOrRd", rev = TRUE),
  ci = 0.95,
  plot_land = FALSE,
  auto_layout = TRUE,
  add = FALSE,
  xlab = "x",
  ylab = "y",
  bg = NULL,
  asp = 2,
  ...
)
}
\arguments{
\item{x}{An object of class \code{admove} or \code{admove_sim}.}

\item{type}{Character string specifying which preference surface to plot:
\code{"taxis"} (default) or \code{"diffusion"}.}

\item{select_cov}{Optional index vector specifying which covariates to plot.
If \code{NULL}, all available covariates for the selected \code{type} are used.}

\item{select.y}{Optional time slice to use from the covariate field. If
\code{NULL}, the first available layer is used.}

\item{select.sea}{Optional index vector specifying which seasonal components
to plot. If \code{NULL}, all available seasons are used.}

\item{combine_cov}{Logical; if \code{TRUE}, preference surfaces are summed across
selected covariates before plotting. Default is \code{FALSE}.}

\item{combine.sea}{Logical; if \code{TRUE}, preference surfaces are summed across
selected seasonal components before plotting. Default is \code{FALSE}.}

\item{main}{Optional main title for the plot panels.}

\item{col}{Colours for the preference surface. Defaults to
\code{hcl.colors(14, "YlOrRd", rev = TRUE)}.}

\item{ci}{Confidence level for pointwise confidence intervals. Default is
\code{0.95}.}

\item{plot_land}{Logical; if \code{TRUE}, land masses are added to the plot.
Default is \code{FALSE}.}

\item{auto_layout}{Logical; if \code{TRUE}, the plotting layout is set
automatically. Default is \code{TRUE}.}

\item{add}{Logical; if \code{TRUE}, the preference surface is added to an
existing plot. If \code{FALSE} (default), a new plot is created.}

\item{xlab}{Label for the x-axis. Default is \code{"x"}.}

\item{ylab}{Label for the y-axis. Default is \code{"y"}.}

\item{bg}{Optional background colour for the plotting device. If \code{NULL}
(default), the current background setting is used.}

\item{asp}{Positive numeric value giving the target aspect ratio
(columns / rows) for multi-panel plot arrangements. Default is \code{2}.}

\item{...}{Additional arguments passed to \code{\link[=plot]{plot()}} when a new plot is
created.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of producing plots.
}
\description{
Plot habitat preference in space for taxis or diffusion as a raster-like
surface over the model grid. Preference surfaces can be shown separately for
selected covariates and seasons, or combined across covariates and/or
seasons.
}
\details{
For fitted objects of class \code{admove}, the function evaluates the estimated
preference functions on the spatial covariate fields and displays the
resulting preference surface for each selected covariate and season.

Preference surfaces can be plotted separately or combined across covariates
(\code{combine_cov = TRUE}) and/or seasons (\code{combine.sea = TRUE}).

For simulated objects of class \code{admove_sim}, the preference surface is
reconstructed from the simulated covariates and parameter values.
}
