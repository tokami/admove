% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{default_map}
\alias{default_map}
\title{Default parameter map}
\usage{
default_map(dat, conf, par)
}
\arguments{
\item{dat}{A data list as produced by \code{\link[=setup_data]{setup_data()}}.}

\item{conf}{A configuration list as produced by \code{\link[=default_conf]{default_conf()}}.}

\item{par}{A parameter list with initial values as produced by
\code{\link[=default_par]{default_par()}}.}
}
\value{
A named list of factors with elements \code{alpha}, \code{logKappa}, \code{beta}, \code{gamma},
and \code{logSdO}. Entries with \code{NA} are fixed, while equal factor levels are
estimated as the same parameter.
}
\description{
Construct the default \code{map} list used to control which parameters are
estimated, fixed, or coupled in TMB. The returned list has the same overall
structure as the model parameter list and is intended to be passed to
\code{TMB::MakeADFun()} via its \code{map} argument.

By default:
\itemize{
\item taxis spline coefficients (\code{alpha}) are mapped using
\code{.make_alpha_map()}, with the first row fixed and remaining coefficients
estimated independently;
\item the taxis scaling parameter (\code{logKappa}) is fixed;
\item diffusion spline coefficients (\code{beta}) are mapped using
\code{.make_beta_map()};
\item advection coefficients (\code{gamma}) are either fixed or, if advection is
enabled, coupled between the \(x\)- and \(y\)-directions within each
covariate;
\item observation-error parameters (\code{logSdO}) are fixed unless estimation
is enabled for the corresponding tag type, in which case \(x\)- and
\(y\)-direction standard deviations are coupled by default.
}
}
\examples{
map <- with(skjepo$sim, default_map(dat, conf, par))

}
