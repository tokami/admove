% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{.sim_one_tag}
\alias{.sim_one_tag}
\title{Simulate a single tag track}
\usage{
.sim_one_tag(
  conf,
  funcs,
  par,
  x0 = NULL,
  y0 = NULL,
  t0 = NULL,
  t1 = NULL,
  dt = 0.1,
  id = NULL,
  tag_type = "d",
  xygrid = NULL,
  nextTo = NULL,
  next_dist = NULL,
  xgr = NULL,
  ygr = NULL,
  celltable = NULL,
  xcen = NULL,
  ycen = NULL,
  sim_engine = 1,
  use_reject = FALSE,
  n_reject = 20,
  ctmc_method = 2
)
}
\arguments{
\item{conf}{A configuration list controlling which movement components are
active, typically produced by \code{\link[=default_conf]{default_conf()}}.}

\item{funcs}{A named list of simulation functions, typically produced by
\code{\link[=default_sim_funcs]{default_sim_funcs()}}. These functions define taxis, diffusion, diffusion
gradient, and advection.}

\item{par}{A parameter list containing model parameters used during
simulation.}

\item{x0}{Numeric scalar giving the release x-coordinate.}

\item{y0}{Numeric scalar giving the release y-coordinate.}

\item{t0}{Numeric scalar giving the release time.}

\item{t1}{Numeric scalar giving the final time of the simulated track.}

\item{dt}{Time step used for simulation.}

\item{id}{Optional tag identifier. If \code{NULL}, a random identifier is
generated.}

\item{tag_type}{Character string giving the tag type. Used to select the
appropriate observation-error component.}

\item{xygrid}{Matrix or data frame of active grid-cell centres. Required for
CTMC simulation.}

\item{nextTo}{Neighbour structure of the grid, as produced for example by
\code{\link[=get_neighbours]{get_neighbours()}}. Required for CTMC simulation.}

\item{next_dist}{Numeric vector of neighbour distances. Required for CTMC
simulation.}

\item{xgr}{Numeric vector of x-direction grid breaks.}

\item{ygr}{Numeric vector of y-direction grid breaks.}

\item{celltable}{Matrix linking grid-cell positions to cell indices.}

\item{xcen}{Numeric vector of x-coordinates of grid-cell centres.}

\item{ycen}{Numeric vector of y-coordinates of grid-cell centres.}

\item{sim_engine}{Integer selecting the simulation engine: \code{1} for
continuous-space simulation and \code{2} for CTMC-based grid simulation.}

\item{use_reject}{Logical; if \code{TRUE}, rejected diffusion proposals are
redrawn when simulated moves leave the valid domain.}

\item{n_reject}{Maximum number of rejection attempts if
\code{use_reject = TRUE}.}

\item{ctmc_method}{Integer controlling the matrix-exponential method used in
CTMC simulation.}
}
\value{
A data frame with simulated tag positions over time. The returned data frame
contains at least the columns \code{t}, \code{x}, \code{y}, and \code{id}.
}
\description{
Simulates the movement trajectory of a single tagged individual under the
\code{admove} movement model. Depending on \code{sim_engine}, movement is
simulated either in continuous space using an Euler-type update or on a
discrete spatial grid using a continuous-time Markov chain (CTMC)
formulation.
}
\details{
For \code{sim_engine = 1}, movement is simulated in continuous space by
combining taxis, advection, and diffusion increments at each time step.

For \code{sim_engine = 2}, movement is simulated on the spatial grid by
constructing a transition-rate matrix from diffusion, taxis, and advection,
and then propagating the tag distribution forward over one time step.

After simulation, observation error is added to all intermediate positions,
while the first and last positions are left unchanged.
}
