% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tags.R
\name{get_recaptured_tags}
\alias{get_recaptured_tags}
\title{Extract recaptured tags}
\usage{
get_recaptured_tags(x, invert = FALSE)
}
\arguments{
\item{x}{An \code{admove_tags} object, or an object containing tags
(\code{admove_data}, \code{admove_sim} or \code{admove}).}

\item{invert}{Logical; if \code{TRUE}, return the tags that were never
recaptured instead. Default: \code{FALSE}.}
}
\value{
An \code{admove_tags} object with all rows of the selected tags, keeping the
spatial and temporal reference of the input.
}
\description{
\code{get_recaptured_tags()} keeps the tags that were observed at least once
after release, i.e. that have a row other than the release with complete
time and position. \code{\link[=prep_ctags]{prep_ctags()}} keeps tags that were never recaptured,
with \code{NA} in the recapture row; \code{\link[=check_tags]{check_tags()}} (called by \code{\link[=setup_data]{setup_data()}})
removes them before fitting. Use this function to get the same tags
earlier, e.g. for plotting, or with \code{invert = TRUE} for the tags that were
never recaptured.
}
\details{
The first row of each tag is its release. A tag counts as recaptured when
any later row has non-missing \code{t}, \code{x} and \code{y}; this also applies to
data-storage and mark-resight tags (observed at least once after release).
}
\examples{
ctags <- skjepo$sim$tags
rec <- get_recaptured_tags(ctags)
length(unique(rec$id))

}
