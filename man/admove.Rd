% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{admove}
\alias{admove}
\title{Fit an admove movement model}
\usage{
admove(
  dat,
  conf = NULL,
  par = NULL,
  map = NULL,
  engine = NULL,
  run = TRUE,
  lower = NULL,
  upper = NULL,
  rel_tol = 1e-10,
  do_predictions = TRUE,
  do_sdreport = TRUE,
  do_report = TRUE,
  do_tag_dist = FALSE,
  save_covariance = FALSE,
  dbg = FALSE,
  control = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{dat}{A data list containing model input data, as produced by
\code{\link[=setup_data]{setup_data()}}.}

\item{conf}{An optional configuration list, typically created by
\code{\link[=default_conf]{default_conf()}}. If \code{NULL}, a default configuration is generated
from \code{dat}.}

\item{par}{An optional named list of initial parameter values, typically
created by \code{\link[=default_par]{default_par()}}. If \code{NULL}, default initial values are
generated from \code{dat} and \code{conf}.}

\item{map}{An optional parameter map, typically created by \code{\link[=default_map]{default_map()}}.
If \code{NULL}, a default map is generated from \code{dat}, \code{conf},
and \code{par}.}

\item{engine}{Optional integer to override \code{conf$engine}. Use
\code{1} for the Kalman filter and \code{2} for the CTMC formulation.
If \code{NULL}, the value in \code{conf} is used.}

\item{run}{Logical; if \code{TRUE} (default), the model is optimized. If
\code{FALSE}, only the RTMB objective object is constructed and returned.}

\item{lower}{Optional lower bounds for optimization. If \code{NULL}, no
explicit lower bounds are supplied.}

\item{upper}{Optional upper bounds for optimization. If \code{NULL}, no
explicit upper bounds are supplied.}

\item{rel_tol}{Relative convergence tolerance passed to \code{\link[stats:nlminb]{stats::nlminb()}}.
Default is \code{1e-10}.}

\item{do_predictions}{Logical; if \code{TRUE} (default), model predictions
are computed after fitting. If \code{FALSE}, prediction-related outputs are
skipped, and some plotting methods may not be available.}

\item{do_sdreport}{Logical; if \code{TRUE} (default), \code{\link[RTMB:sdreport]{RTMB::sdreport()}} is
run to obtain uncertainty estimates for model parameters and derived
quantities.}

\item{do_report}{Logical; if \code{TRUE} (default), \code{obj$report()} is
run to extract reported RTMB quantities.}

\item{do_tag_dist}{Logical; if \code{TRUE}, predicted location distributions
are precomputed for all tags via \code{\link[=add_tag_dist]{add_tag_dist()}} and stored in
\code{fit$tag_dist} (consumed by \code{\link[=plot_tag_dist]{plot_tag_dist()}}). Default is
\code{FALSE}, as this is per-tag expensive and only needed for tag-location
visualisation; it requires a prediction grid and runs after the steps
above. Compute distributions for selected tags later with
\code{add_tag_dist(fit, i = ...)}.}

\item{save_covariance}{Logical; if \code{TRUE}, the covariance matrix from
\code{\link[RTMB:sdreport]{RTMB::sdreport()}} is retained. This may substantially increase memory use.}

\item{dbg}{Logical; if \code{TRUE}, the function is run in debugging mode.
Default is \code{FALSE}.}

\item{control}{An optional named list of control settings passed to the
optimizer.}

\item{verbose}{Logical; if \code{TRUE}, progress messages are printed.}

\item{...}{Additional arguments passed to \code{\link[RTMB:MakeADFun]{RTMB::MakeADFun()}}.}
}
\value{
A fitted model object of class \code{"admove"}. Depending on the function
arguments, the returned object may include the RTMB objective function, the
optimization output, reported quantities, predictions, and uncertainty
estimates.
}
\description{
Fits the \code{admove} movement model to tagging data to estimate movement
processes and, where applicable, habitat preference and advection effects.
The model supports different types of tagging data and can be fitted using
either a Kalman filter (continuous space, discrete time) or a continuous-time
Markov chain (CTMC; discrete space, continuous time) formulation.
}
\details{
This is the main model-fitting function in \code{admove}. It combines the
supplied data, configuration, parameter values, and parameter mapping,
constructs an RTMB objective function, and optionally optimizes it using
\code{\link[stats:nlminb]{stats::nlminb()}}.

If configuration settings, initial parameter values, or parameter maps are
not supplied, they are generated automatically using \code{\link[=default_conf]{default_conf()}},
\code{\link[=default_par]{default_par()}}, and \code{\link[=default_map]{default_map()}}, respectively.
}
\examples{
fit <- admove(skjepo$sim, do_sdreport = FALSE)

}
