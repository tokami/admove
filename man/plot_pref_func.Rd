% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_pref_func}
\alias{plot_pref_func}
\title{Plot habitat preference functions}
\usage{
plot_pref_func(
  x,
  type = "taxis",
  select = NULL,
  main = NULL,
  cols = .admove_cols(10),
  lwd = 1,
  ci = 0.95,
  auto_layout = TRUE,
  add = FALSE,
  panel_lab = NULL,
  xlab = NULL,
  ylab = "Preference",
  bg = NULL,
  ylim = NULL,
  xlim = NULL,
  return_limits = FALSE,
  data.range = FALSE,
  asp = 2,
  leg_ncol = 1,
  ...
)
}
\arguments{
\item{x}{An object of class \code{admove} or \code{admove_sim}.}

\item{type}{Character string specifying which preference function to plot:
\code{"taxis"} (default) or \code{"diffusion"}.}

\item{select}{Optional index vector specifying which covariates to plot. If
\code{NULL}, all available covariates for the selected \code{type} are shown.}

\item{main}{Optional main title. Can be a single character string or a
character vector with one title per panel. If \code{NULL}, covariate names are
used where available.}

\item{cols}{Colours used for the plotted preference functions. Defaults to
\code{admove:::.admove_cols(10)}.}

\item{lwd}{Line width. Default is \code{1}.}

\item{ci}{Confidence level for pointwise confidence intervals. Default is
\code{0.95}.}

\item{auto_layout}{Logical; if \code{TRUE}, the plotting layout is set
automatically. Default is \code{TRUE}.}

\item{add}{Logical; if \code{TRUE}, the preference function is added to an
existing plot. If \code{FALSE} (default), a new plot is created.}

\item{panel_lab}{Optional character string added as a panel label in the
top-left corner of the plot. Default is \code{NULL}.}

\item{xlab}{Label for the x-axis. If \code{NULL} (default), the covariate name
from \code{dat$cov} is used for each panel; falls back to \code{"Covariate"} when no
name is available.}

\item{ylab}{Label for the y-axis. Default is \code{"Preference"}.}

\item{bg}{Optional background colour for the plotting device. If \code{NULL}
(default), the current background setting is used.}

\item{ylim}{Optional y-axis limits.}

\item{xlim}{Optional x-axis limits.}

\item{return_limits}{Logical; if \code{TRUE}, no plot is produced and a list with
\code{xlim} and \code{ylim} is returned instead. Default is \code{FALSE}.}

\item{data.range}{Logical; if \code{TRUE}, x-axis limits are based on the range of
the observed covariate data rather than the prediction grid. Default is
\code{FALSE}.}

\item{asp}{Positive numeric value giving the target aspect ratio
(columns / rows) for multi-panel plot arrangements. Default is \code{2}.}

\item{leg_ncol}{Number of columns in the legend when seasonal curves are
shown. Default is \code{1}.}

\item{...}{Additional arguments passed to \code{\link[=plot]{plot()}} when a new plot is
created.}
}
\value{
Invisibly returns \code{NULL} when plotting. If \code{return_limits = TRUE}, returns a
list with components \code{xlim} and \code{ylim}.
}
\description{
Plot estimated or simulated habitat preference functions against covariate
values for taxis or diffusion. The function can display one or several
covariate-specific preference functions, optionally with confidence bands for
fitted \code{admove} objects.
}
\details{
For fitted objects of class \code{admove}, the function plots estimated
preference functions based on predicted values stored in the fitted object.
If standard deviations are available, pointwise confidence bands are drawn.

For simulated objects of class \code{admove_sim}, the function reconstructs the
corresponding preference function directly from the simulated spline knots
and coefficients.

If multiple seasonal curves are available for a covariate, they are shown as
separate line types, and a legend is added.
}
