% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates.R
\name{subset-admove_cov}
\alias{subset-admove_cov}
\alias{[.admove_cov}
\title{Subset an \code{admove_cov} object}
\usage{
\method{[}{admove_cov}(x, i, j, k, ..., drop = TRUE)
}
\arguments{
\item{x}{An object of class \code{admove_cov}.}

\item{i}{Indices for the first dimension.}

\item{j}{Indices for the second dimension.}

\item{k}{Indices for the third dimension, typically corresponding to time.}

\item{...}{Further indices passed to the underlying array subsetting
operation.}

\item{drop}{Logical; should dimensions of length one be dropped? Default:
\code{TRUE}.}
}
\value{
An object of class \code{admove_cov} if the subset retains three dimensions;
otherwise a regular subsetted R object.
}
\description{
Subsetting method for objects of class \code{admove_cov}.

This method preserves the \code{admove_cov} class and associated attributes when
the result remains a three-dimensional covariate array. If subsetting
returns an object with fewer than three dimensions, the result is returned as
a regular R object without the \code{admove_cov} class.

In particular, subsetting only along the third dimension (time) returns a
subsetted \code{admove_cov} object with the corresponding time-related attributes
updated where available.
}
\details{
The method first performs standard array subsetting on the unclassed object.
If the resulting object still has three dimensions, attributes other than
\code{dim} and \code{dimnames} are restored and the original class is reattached.
}
