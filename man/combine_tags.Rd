% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tags.R
\name{combine_tags}
\alias{combine_tags}
\alias{c.admove_tags}
\title{Combine \code{admove_tags} objects}
\usage{
combine_tags(..., recursive = FALSE)

\method{c}{admove_tags}(..., recursive = FALSE)
}
\arguments{
\item{...}{Objects of class \code{admove_tags}, or a single list containing such
objects.}

\item{recursive}{Ignored. Included for compatibility with the generic
\code{\link[base:c]{base::c()}} interface.}
}
\value{
A single object of class \code{admove_tags} containing all rows from the input
objects.
}
\description{
\code{combine_tags()} combines multiple objects of class \code{admove_tags} into a
single \code{admove_tags} object. It is designed to support workflows such as
combining data-storage, mark-resight, and mark-recapture tags into one
unified data set.

All inputs must inherit from \code{admove_tags} and must have compatible spatial
and temporal reference information.
}
\details{
Before combining inputs, the function:
\itemize{
\item removes \code{NULL} inputs,
\item checks that all remaining inputs inherit from \code{admove_tags},
\item aligns columns across objects by filling missing columns with \code{NA},
\item checks that all objects have the same \code{sref}, and
\item checks that all objects have the same \code{tref}.
}

If spatial or temporal reference information differs across inputs, the
function stops with an error.
}
\examples{
## tags <- combine_tags(dtags, stags, ctags)
## tags <- c(dtags, ctags)

}
